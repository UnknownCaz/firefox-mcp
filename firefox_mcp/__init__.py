"""Firefox Browser Control MCP server.

Attach-only WebDriver BiDi client + MCP tools that let Claude operate the
user's own already-running Firefox. Never launches or closes Firefox.
"""

__version__ = "0.1.0"
