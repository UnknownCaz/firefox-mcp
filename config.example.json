{
  "_comment": "Copy to ~/.config/firefox-mcp/config.json (on Windows: %USERPROFILE%\\.config\\firefox-mcp\\config.json). Both lists are optional. Entries are matched as case-insensitive substrings of the tab's hostname.",

  "blockedDomains": [
    "bank",
    "paypal.com",
    "venmo.com",
    "coinbase.com",
    "wellsfargo.com",
    "chase.com"
  ],

  "allowedDomains": []
}
